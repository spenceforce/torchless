"""Torchless: a neural network library built from scratch.

This package is the code companion to the Torchless book. It implements
the core building blocks of a neural network — values, gradients, and
eventually layers and optimizers — using only plain Python, so every
concept is visible and traceable.
"""
from .base.constant import Constant
from .base.value import Value
